#### Prerequisites
- OpenGL driver
    ```bash
    sudo pacman -Sy mesa
    ```
- C++ compiler
    ```bash
    sudo pacman -Sy g++
    ```

#### Template
```bash
git clone -b template --single-branch https://github.com/ridwanalmahmud/openglbasics.git
```
